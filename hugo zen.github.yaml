---
title: "Stille und Papier"
baseURL: "https://tamarue.github.io/sup"
theme: "zen"
languageCode: "de-DE"

sectionPagesMenu: main

cookieConsent: true       # Show cookie consent form, default false.
dateformat: 2. Januar 2006            # Set the date format, default to "2 January, 2006"
feedlinks: true           # Show feed links in the footer.
menuInHeader: true        # Move the main menu to the header, default false.
mobileMenu: true          # Turn on a mobile menu on small screens, default false.
poweredby: true           # Show powered by hugo in footer
privacyPolicyUrl: ""      # If set will add link to cookie consent form.
sidebar: "false"             # Show a sidebar to the right, default false.
submitted: true           # Show author and date information for a post.



defaultContentLanguage: "de"
pluralizelisttitles: false
removePathAccents: true
rssLimit: 20
pagination:
  pagerSize: 10
  path: page

outputFormats:
  SearchIndex:
    mediaType: "application/json"
    baseName: "searchindex"
    isPlainText: true
    notAlternative: true

outputs:
  home: ["HTML","RSS", "JSON", "SearchIndex"]
  section: ["HTML","RSS", "JSON"]

params:
  contact: "info@example.org"
  description: ""
  dateformat: "2 January 2006"
  favicon: "apple-touch-icon.png"
  feedlinks: true
  icon: "apple-touch-icon.png"
  image: "apple-touch-icon.png"
  imageMaxWidth: 400
  logoWidth: 64
  mobileMenu: true
  poweredby: true
  realfavicongenerator: true
  sidebar: true
  submitted: true
  author:
    name: "tamarue"
    url: "https://example.org/somepage"
    avatar: "path/to/some-image.jpg"
